/* 
  (async () => {
    const newTown = {
      "data":{
          "route": "COCORNA - GRANADA - BARBOSA (ANTIOQUIA) - EPM",
          "viatic": $$$,
          "salary": $$$,
      }
  }
    const res = await postTown(newTown);
    if(!res.loading){
      console.log(res);
    }
  })();
*/